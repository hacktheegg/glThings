@PACKAGE_INIT@

if(NOT DEFINED PACKAGE_PREFIX_DIR)
    set(PACKAGE_PREFIX_DIR "${CMAKE_INSTALL_PREFIX}")
endif()

message(STATUS "Uninstalling \"${PACKAGE_NAME}\"")
foreach(file ${INSTALL_FILES})
    file(REMOVE "${PACKAGE_PREFIX_DIR}/${file}")
endforeach()

message(STATUS "Uninstall completed")
